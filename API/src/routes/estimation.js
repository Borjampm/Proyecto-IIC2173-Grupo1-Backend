const Router = require('koa-router');

const router = new Router();

router.post('estimation.create', '/new', async (ctx) => {
    request = ctx.request.body;

    
});

router.get('estimation.show', '/:user', async (ctx) => {

});